<% unless @current_product.nil?%>
  product = document.getElementById("<%=@current_product.id%>")
  product.className = "card-product product-highlight-red"
  setTimeout(function(){ product.className = "card-product" }, 1000);
<%end%>
